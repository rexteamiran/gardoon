# 🤖 agents.md — قرارداد توسعه گردون

> این فایل مرجع اصلی برای **هر توسعه‌دهنده‌ای** است که روی گردون کار می‌کند — انسانی یا هوش مصنوعی (Claude Code، Cursor، Copilot و...).
> هر تغییر در کد باید با قوانین این فایل هماهنگ باشد.

---

## ۱. هویت پروژه — هرگز تغییر نکنید

| مورد | مقدار ثابت |
|------|-------------|
| نام اپ | گردون |
| نام فرمت فایل سناریو | `.grdn` |
| نام پکیج | `ir.gardoon.app` |
| فریم‌ورک | Flutter — فقط اندروید |
| حداقل SDK | API 24 (Android 7) |
| زبان رابط کاربری | فقط فارسی (RTL) |
| حالت اتصال نسخه ۱ | کاملاً آفلاین — ممنوعیت هر kind of network call |

⚠️ اگر درخواستی با جدول بالا تناقض داشت، **قبل از اجرا توقف کن و تذکر بده.**

---

## ۲. قانون طلایی برای دستیارهای AI

1. **ساده توضیح بده.** مخاطب اصلی این پروژه توسعه‌دهنده‌ای با تجربه تقریباً صفر است. هر توضیح باید برای یک تازه‌کار قابل فهم باشد.
2. **هرگز چند مفهوم پیچیده را با هم رها نکن.** هر پاسخ حداکثر یک مفهوم جدید آموزش می‌دهد.
3. **قبل از کدنویسی بزرگ، طرح بده.** برای هر تغییر بزرگ‌تر از یک فایل، اول ساختار را توضیح بده و بعد کد بنویس.
4. **کد کامل و قابل اجرا بده.** تکه‌کد ناقص با `// بقیه اینجا...` ممنوع.
5. **هر فایل جدید را در جای درست ساختار پروژه** (بخش ۳) **بساز.** فایل سرگردان ممنوع.
6. **وابستگی جدید (package) فقط با اجازه.** قبل از افزودن هر پکیج به `pubspec.yaml`، دلیل، جایگزین و پاپولاریتی آن را توضیح بده.
7. **حدس نزن.** اگر مشخصات چیزی در این مستندات نبود، بپرس؛ آن‌گه پیشنهاد بده و بعد از تأیید اجرا کن.
8. **مستندات را به‌روز کن.** هر فیچر جدید باید در فایل مرتبط (`architecture.md` یا `scenario-format.md` یا `roadmap.md`) ثبت شود.

---

## ۳. ساختار پروژه

```
gardoon/
├── agents.md                  ← همین فایل
├── README.md
├── architecture.md
├── scenario-format.md
├── design.md
├── roadmap.md
│
├── android/                   ← تنظیمات اندروید (package name، آیکون، intent filter)
├── assets/
│   ├── icons/                 ← آیکون‌های داخلی نقش‌ها (SVG)
│   ├── sounds/                ← افکت‌های صوتی داخلی
│   ├── scenarios/             ← ۳ سناریوی رایگان + ۳ سناریوی تبلیغی (فرمت .grdn)
│   └── fonts/                 ← فونت فارسی
│
├── lib/
│   ├── main.dart              ← نقطه ورود
│   ├── app.dart               ← MaterialApp + تم + مسیریابی
│   │
│   ├── core/                  ← ابزارهای پایه (بدون منطق بازی)
│   │   ├── theme/             ← تم تاریک و روشن + پالت گویی
│   │   ├── widgets/           ← ویجت‌های مشترک (دکمه گویی، کارت، تراشه...)
│   │   ├── effects/           ← افکت‌های گویی و انیمیشن
│   │   ├── extensions/        ← extensionهای Dart
│   │   └── constants.dart     ← اعداد ثابت (مدت تایمر پیش‌فرض و...)
│   │
│   ├── engine/                ← ⭐ موتور بازی — خالص و بدون UI
│   │   ├── models/            ← مدل‌های داده (Scenario, Role, Phase, Action...)
│   │   ├── state_machine.dart ← ماشین حالت بازی (NIGHT → DAWN → DAY → VOTE...)
│   │   ├── action_executor.dart ← اجرای بلوک‌های توانایی
│   │   ├── win_checker.dart   ← ارزیابی شرط‌های برد
│   │   ├── role_allocator.dart← توزیع نقش (نقش‌کشه)
│   │   ├── validator.dart     ← اعتبارسنجی سناریو (خطا + هشدار + ترازو)
│   │   └── json_codec.dart    ← تبدیل سناریو ↔ JSON (فرمت .grdn)
│   │
│   ├── grdn/                  ← کار با فایل .grdn
│   │   ├── grdn_reader.dart   ← خواندن ZIP + migration نسخه‌ها
│   │   ├── grdn_writer.dart   ← ساخت ZIP + checksum
│   │   ├── migration/         ← مهاجرت‌های نسخه فرمت (v1_to_v2.dart و...)
│   │   └── share.dart         ← اشتراک‌گذاری فایل + QR + دریافت فایل خارجی
│   │
│   ├── features/              ← صفحات اپ — هر فیچر یک پوشه
│   │   ├── home/              ← صفحه اصلی (خانه گردون)
│   │   ├── table/             ← 🃏 میزگرد — اجرای بازی
│   │   ├── role_deal/         ← 🎲 نقش‌کشه
│   │   ├── builder/           ← 🛠 سناریوساز — ۴ سطح
│   │   │   ├── level1_basic/  ← فرم‌محور (رایگان)
│   │   │   ├── level2_abilities/ ← بلوک‌ساز توانایی (طلایی)
│   │   │   ├── level3_win/    ← شرط‌برد بصری (طلایی)
│   │   │   └── level4_events/ ← رویدادهای تصادفی (طلایی)
│   │   ├── library/           ← لیست سناریوها (باندل + من)
│   │   └── premium/           ← گردون طلایی (پرداخت کافه بازار)
│   │
│   ├── data/                  ← ذخیره‌سازی
│   │   ├── settings.dart      ← تنظیمات (shared_preferences)
│   │   ├── scenario_store.dart← ذخیره/بارگذاری سناریوها از فایل
│   │   └── stats.dart         ← آمار بازی‌ها
│   │
│   └── l10n/                  ← رشته‌های فارسی (فقط در نسخه‌های بعدی چندزبانه؛ فعلاً رشته‌ها inline)
│
└── test/                      ← تست‌ها — همیشه با موتور بازی شروع می‌شود
    ├── engine_test.dart
    ├── grdn_test.dart
    └── validator_test.dart
```

### قاعده پوشه‌بندی

- هر فیچر داخل `features/` خودکفا است: `widgets/`, `screens/`, `providers/`
- **موتور بازی (`engine/`) هرگز import از `features/` ندارد** — فقط مدل و منطق خالص
- رشته فارسی مستقیم در کد مجاز است (تک‌زبانه‌ایم) ولی در `widgets` مشترک، پارامتر بگیرد

---

## ۴. قوانین کدنویسی Dart/Flutter

### نام‌گذاری

| مورد | قانون | مثال |
|------|-------|------|
| کلاس/تایپ | UpperCamelCase | `ScenarioCard`, `RoleAllocator` |
| متغیر/تابع | lowerCamelCase | `nightPhase`, `allocateRoles()` |
| فایل | snake_case | `role_allocator.dart`, `grdn_reader.dart` |
| ثابت | lowerCamelCase + `const` | `defaultTimerSeconds` |
| مدل‌های فرمت .grdn | پیشوند `Grdn` | `GrdnManifest`, `GrdnScenario` |

### قوانین اجباری

1. **`flutter analyze` با صفر خطا** — قبل از هر commit
2. **null-safety کامل** — `!` (bang operator) فقط با کامنت دلیل
3. **کامنت‌ها فارسی** — توضیح «چرا»، نه «چه»
   ```dart
   // شب اول کشتن ندارد چون بازیکن‌ها هنوز نقش‌ها را نشناخته‌اند
   if (state.nightNumber == 0) skipKillActions();
   ```
4. **ویجت‌های بلند تقسیم شوند** — هر متد `build` حداکثر ~۸۰ خط
5. **`const` حداکثری** — برای عملکرد بهتر
6. **منطق در provider/controller** — ویجت‌ها فقط UI؛ محاسبه در لایه engine
7. **RTL آگاه باش** — چپ/راست دستی ممنوع؛ از `start`/`end` استفاده کن
8. **اعداد فارسی در UI** — تبدیل با helper: `'۳ دقیقه'` نه `'3 دقیقه'`

### ممنوعیت‌ها

- ❌ هیچ package مربوط به شبکه/HTTP در نسخه ۱
- ❌ هاردکد کردن قوانین بازی در کد UI — همه‌چیز از مدل `Scenario` می‌آید
- ❌ استایل inline پراکنده — رنگ و اندازه از `Theme` و `design.md`
- ❌ `setState` برای state پیچیده — از Riverpod استفاده شود

---

## ۵. قراردادهای معماری

### جریان داده

```
UI (Widget)
   ↓ فراخوانی
Controller / Riverpod Notifier
   ↓ فراخوانی
Engine (state_machine, action_executor, win_checker)
   ↓ تغییر وضعیت
GameSessionState (immutable)
   ↑ برگشت به UI از طریق provider
```

- وضعیت بازی **immutable** است — هر تغییر، state جدید می‌سازد
- موتور بازی از فلاتر مستقل است و باید **بدون UI قابل تست** باشد
- هر event موتور (شروع شب، مرگ بازیکن، پایان بازی) یک `GameEvent` تولید می‌کند که UI و صدا از آن مصرف می‌کنند

### فرمت .grdn

- مدل‌های Dart فقط با `Grdn` شروع می‌شوند و در `engine/models/` زندگی می‌کنند
- تبدیل JSON ↔ مدل فقط از `json_codec.dart` — هیچ جای دیگری دستی JSON نمی‌سازد
- هر تغییر اسکیمای JSON = افزایش `formatVersion` + نوشتن migration (بخش کامل در `scenario-format.md`)

### تم و رنگ

- هرگز رنگ هاردکد (`Color(0xFF...)`) در ویجت‌ها — فقط از `AppColors` (در `core/theme/`)
- دو تم تاریک/روشن طبق `design.md` — هر دو باید تعریف شوند حتی اگر فعلاً یکی استفاده شود

---

## ۶. گردن‌بند قفل فیچرها (Premium Gate)

هر قابلیت طلایی باید از یک نقطه مرکزی عبور کند:

```dart
// core/premium_gate.dart
class PremiumGate {
  /// آیا کاربر به فیچر دسترسی دارد؟
  static bool canUse(PremiumFeature feature);
}
```

- `PremiumFeature.builderLevel2/3/4`, `.premiumScenarios`, `.noAds`, ...
- هیچ صفحه‌ای مستقیماً اشتراک را چک نمی‌کند — فقط `PremiumGate`
- دلیل: بعداً ممکن است مدل اشتراک (یک‌ساله) تغییر کند؛ فقط یک فایل عوض می‌شود

---

## ۷. تست

| چه چیزی | چطور |
|----------|-------|
| موتور بازی (اجباری) | unit test بدون فلاتر — سناریوی نمونه را اجرا کن و وضعیت‌ها را چک کن |
| اعتبارسنجی سناریو | unit test — هر قانون خطا/هشدار یک تست |
| خواندن/نوشتن .grdn | roundtrip test — بنویس ← بخوان ← مساوی بودن |
| migration فرمت | فایل نسخه قدیمی ذخیره‌شده در `test/fixtures/` |
| UI | فعلاً دستی — طبق چک‌لیست `roadmap.md` |

---

## ۸. Git

- نام شاخه: `feature/builder-level2`, `fix/timer-reset`, ...
- پیام commit فارسی یا انگلیسی آزاد ولی توصیفی: `موتور شب: اجرای ترتیب بیداری`
- هر commit باید `flutter analyze` تمیز و تست‌های سبز داشته باشد

---

## ۹. تعریف «تمام‌شده» برای هر فیچر

- [ ] کد مطابق این قرارداد
- [ ] `flutter analyze` صفر خطا
- [ ] تست موتور مرتبط سبز
- [ ] روی گوشی واقعی Android 7+ تست شده
- [ ] تم تاریک و روشن هر دو درست
- [ ] رشته‌های فارسی درست و بدون نصفه‌نیمزه RTL
- [ ] مستندات مربوطه به‌روز شده
