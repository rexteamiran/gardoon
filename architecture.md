# 🏗 architecture.md — معماری فنی گردون

> این فایل توضیح می‌دهد گردون **از داخل چطور کار می‌کند**. به‌صورت ساده و قدم‌به‌قدم نوشته شده.

---

## ۱. تصویر کلی

```
┌─────────────────────────────────────────────────────┐
│                     📱 گردون                         │
│                                                     │
│  ┌───────────┐  ┌───────────┐  ┌───────────────┐   │
│  │ 🃏 میزگرد  │  │ 🛠 سناریوساز│  │ 🏛 کتابخانه    │   │
│  │  (اجرای   │  │  (ساختن    │  │  (سناریوها)    │   │
│  │   بازی)   │  │   سناریو)  │  │               │   │
│  └─────┬─────┘  └─────┬─────┘  └──────┬────────┘   │
│        │              │               │            │
│        ▼              ▼               ▼            │
│  ┌─────────────────────────────────────────────┐   │
│  │        ⚙️ موتور بازی (engine/) — خالص       │   │
│  │  state_machine • action_executor •          │   │
│  │  win_checker • validator • role_allocator   │   │
│  └────────────────────┬────────────────────────┘   │
│                       │                            │
│  ┌────────────────────▼────────────────────────┐   │
│  │     📦 لایه داده — مدل Scenario + JSON      │   │
│  └────────────────────┬────────────────────────┘   │
│                       │                            │
│  ┌────────────────────▼────────────────────────┐   │
│  │   💾 ذخیره‌سازی: فایل .grdn + تنظیمات        │   │
│  └─────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────┘
```

**اصل مهم:** موتور بازی هیچ اطلاعی از UI ندارد. یک مدل `Scenario` می‌گیرد و بازی را اجرا می‌کند. به همین دلیل:
- سناریوهای پیش‌فرض اپ و سناریوهای کاربر **هر دو از یک راه ساخته و اجرا می‌شوند**
- در آینده شبیه‌ساز بات هم همین موتور را صدا می‌زند

---

## ۲. پشته فناوری

| بخش | انتخاب | چرا |
|-----|--------|------|
| فریم‌ورک | Flutter 3.x | تک‌کد، ابزار UI قوی، RTL خوب |
| زبان | Dart 3 | null-safe، pattern matching |
| State | Riverpod | ساده برای شروع، مقیاس‌پذیر |
| مسیریابی | go_router | استاندارد فعلی فلاتر |
| ZIP (.grdn) | archive | ساخت/خواندن ZIP خالص Dart |
| JSON | json_serializable | تولید کد تبدیل |
| تنظیمات | shared_preferences | تم، صدا، وضعیت طلایی |
| پرداخت | package کافه بازار (in-app billing v4) | اشتراک یک‌ساله گردون طلایی |
| تبلیغات | تپسل / کافه بازار | ۳ سناریوی تبلیغی |
| QR | qr_flutter + mobile_scanner | ساخت و خواندن QR سناریو |
| فونت | وزیرمتن (Vazirmatn) | فارسی زیبا، رایگان، اعداد فارسی |

---

## ۳. موتور بازی — قلب گردون

### ۳.۱ ماشین حالت (State Machine)

```
                    ┌──────────────────────────────────┐
                    ▼                                  │
  ┌──────┐   ┌──────────┐   ┌─────────┐   ┌──────────┐│
  │ LOBBY│──▶│NIGHT_    │──▶│  DAWN   │──▶│DAY_      ││
  │(میز) │   │ACTIONS   │   │(اعلام   │   │DISCUSSION││
  └──────┘   │(به ترتیب │   │مردها)   │   └────┬─────┘│
             │ سناریو)  │   └─────────┘        ▼      │
             └──────────┘                ┌──────────┐  │
                    ▲                    │   VOTE   │  │
                    │                    └────┬─────┘  │
                    │      CHECK_WIN ◀────────┘        │
                    │        │                         │
                    │   ┌────┴─────┐                   │
                    │   ▼          ▼                   │
                    │ FINISHED   (ادامه ← برگشت به شب)─┘
                    └─ هر فاز پس از پایان شرط برد را چک می‌کند
```

هر حالت یک enum:

```dart
enum GamePhaseType { lobby, nightActions, dawn, dayDiscussion, vote, finished }
```

**نکته کلیدی:** حالت‌ها از سناریو خوانده می‌شوند، نه هاردکد. فازهای اضافی (مثلاً «محاکمه») در سناریو تعریف می‌شوند و ماشین حالت به‌صورت داده‌محور آن‌ها را می‌چرخاند.

### ۳.۲ وضعیت بازی (GameSessionState)

```dart
class GameSessionState {
  final GrdnScenario scenario;          // سناریوی در حال اجرا
  final List<LivePlayer> players;       // بازیکن‌ها + نقش + وضعیت
  final GamePhaseType currentPhase;     // فاز فعلی
  final int roundNumber;                // شب/روز چندم
  final List<PendingAction> pendingActions; // اکشن‌های صف‌شده شب
  final List<ResolvedEvent> history;    // تاریخچه — برای لاگ و وصیت
}
```

- **Immutable** — هر رویداد، state جدید می‌سازد (کپی با تغییر)
- UI با Riverpod این state را گوش می‌دهد و رندر می‌کند

### ۳.۳ اجرای اکشن‌ها (Action Executor)

هر بلوک توانایی در سناریو یک `AbilityBlock` است. در شب:

1. ماشین حالت بلوک‌ها را بر اساس `priority` (اسلایدر ۱-۲۰) مرتب می‌کند
2. برای هر بلوک، UI صفحه هدف‌گیری نشان می‌دهد → گرداننده هدف را تأیید می‌کند
3. `action_executor.dart` نتیجه را **در صف** ثبت می‌کند (نه فوراً اعمال)
4. پایان شب: صف با احترام به محافظت‌ها/مصونیت‌ها resolve می‌شود → مرگ‌ها تعیین می‌شوند

**چرا صف؟** چون اولویت‌ها به هم وابسته‌اند — مثلاً اگر دکتر بعد از تیرانداز بیدار شود ولی محافظتش قبل از تیر باید اعمال شود. صف + قواعد resolve تضمین می‌کند هر سناریوی عجیبی هم درست کار کند.

### ۳.۴ شرط برد (Win Checker)

```dart
// شرط‌ها به‌صورت درخت شرطی از سناریو می‌آیند:
// WinCondition(team, terms: [Term(operand, comparator, value)], join: AND/OR)
```

- بعد از **هر مرگ** (شب یا روز) همه شرط‌ها ارزیابی می‌شوند
- چند تیم می‌توانند هم‌زمان شرط بردشان را داشته باشند (برد مشترک فرقه+مافیا ممکن است)

---

## ۴. سناریوساز — چهار سطح

| سطح | چیست | UI | دسترسی |
|------|-------|-----|---------|
| **۱ — پایه** | نام، آیکون، تیم‌ها، تعداد نقش‌ها، ترتیب شب (لیست جابه‌جاشو)، تنظیمات رأی/روز، انتخاب شرط برد از پریست | فرم‌ها + درگ‌ودراپ ساده | رایگان |
| **۲ — بلوک‌ساز توانایی** | برای هر نقش: افزودن بلوک‌های حمله/نجات/بازرسی/... با تنظیم کامل (هدف‌گیری، محدودیت، اولویت) | شیت بلوک‌ها + پیش‌نمایش زنده کارت | 🟡 طلایی |
| **۳ — شرط‌برد بصری** | فرمول‌ساز برد با تراشه‌های لمسی (عملوند/عملگر/مقدار) — تیم‌های سفارشی با شرط خودشان | بلوک‌های فرمول | 🟡 طلایی |
| **۴ — رویدادهای تصادفی** | دک رویداد: هر شب × رویداد رندوم (قطعی برق، سکوت، دوقتله...) با تاثیر در فازها | کارت‌ساز رویداد | 🟡 طلایی |

**اعتبارسنجی زنده:** در همه سطوح، پنل پایین همیشه سه چیز را نشان می‌دهد:
- 🔴 خطاها (مانع خروجی فایل می‌شوند)
- 🟡 هشدارها (مجاز ولی خطرناک)
- ⚖️ گیج ترازو (وزن تقریبی تیم‌ها بر اساس وزن هر بلوک — وزن هر نوع بلوک در constants)

---

## ۵. فرمت .grdn — گردش کار

### نوشتن:
```
مدل Scenario (Dart)
  → json_codec.dart → data.grdn.json
  → grdn_writer.dart → ZIP { manifest + data + assets }
  → checksum (SHA-256) → فایل .grdn نهایی
  → ذخیره در پوشه اپ + اشتراک (intent) + QR (Base64 فشرده، فقط سناریوهای بدون asset سنگین)
```

### خواندن:
```
فایل .grdn (از باز کردن با / QR / داخل اپ)
  → grdn_reader.dart → بازکردن ZIP
  → بررسی checksum → تطبیق formatVersion
  → اگر قدیمی: زنجیره migration (v1→v2→v3...) → جدید
  → اعتبارسنجی → مدل Scenario → ذخیره در کتابخانه
```

**Intent Filter اندروید:** اپ برای پسوند `.grdn` و MIME سفارشی `application/x-gardoon-scenario` ثبت می‌شود تا «باز کردن با گردون» در تلگرام/فایل‌منیجر ظاهر شود.

**سناریوی قفل:** اگر `locked=true` باشد، خواننده فقط metadata + نسخه «پخته» (roles به‌صورت شناسه بی‌نام) را نمایش می‌دهد؛ نقش‌ها هنگام شروع بازی توزیع می‌شوند و تا لحظه دیدن کارت، مخفی می‌مانند.

---

## ۶. ذخیره‌سازی

| داده | کجا | چرا |
|------|------|------|
| سناریوهای باندل | `assets/scenarios/*.grdn` | همراه اپ، readonly |
| سناریوهای کاربر | `getApplicationDocumentsDirectory()/scenarios/*.grdn` | فایل واقعی = قابل خروجی/پشتیبان |
| ایندکس کتابخانه | فایل `library_index.json` (نام، آیدی، تاریخ، قفل) | لیست سریع بدون بازکردن همه ZIPها |
| تنظیمات | shared_preferences | تم، صدا، زبان اعداد |
| وضعیت طلایی | shared_preferences + تأیید دوره‌ای با کافه بازار | جلوگیری از کرک ساده |
| آمار بازی | `stats.json` | تعداد بازی، برد تیم‌ها — برای آینده |

بدون SQLite در نسخه ۱ — داده‌ها ساختار ساده‌ای دارند و فایل‌محور با ماهیت سناریو (یک فایل = یک سناریو) هماهنگ‌تر است.

---

## ۷. پرداخت و تبلیغ

### گردون طلایی (اشتراک یک‌ساله)
- محصول in-app کافه بازار: `subscription_golden_year`
- جریان: صفحه premium → خرید کافه بازار → تأیید → ذخیره وضعیت + تاریخ انقضا
- در هر اجرای اپ: چک سریع اعتبار (کوئری سبک به سرویس بازار) — اگر بازار در دسترس نبود، تا ۷ روز به وضعیت قبلی اعتماد می‌شود (رفتار آفلاین‌پسند)
- گیت: همه دسترسی‌ها فقط از `PremiumGate`

### تبلیغ
- فقط در: باز کردن ۳ سناریوی ویژه (ویدیوی rewarded) + بنر در صفحات غیربازی (کتابخانه، سناریوساز سطح ۱)
- **هرگز وسط میزگرد** — تجربه بازی مقدس است

---

## ۸. مدیریت صدا

- کتابخانه داخلی: تق تق نقالی/چرخش برای تغییر فاز، جیرجیر شب، صدای رأی‌گیری، مرگ، پیروزی
- افکت‌ها با `audioplayers` — پیش‌بارگذاری در شروع میزگرد برای پخش بدون تأخیر
- صداها از `GameEvent` موتور مصرف می‌شوند: هر event نوع صدای خودش را دارد
- کلید خاموش/روشن در تنظیمات + حافظه

---

## ۹. افکت گویی — پیاده‌سازی فلاتر

دو مسیر بر اساس قدرت گوشی (تشخیص با یک‌بار بنچمارک سبک در اولین اجرا):

| مسیر | گوشی | تکنیک |
|------|------|-------|
| پیشرفته | API 29+ | شیدر AGSL (Fragment Shader) — blur + contrast threshold روی لایه عناصر |
| سازگار | API 24-28 | BackdropFilter (ImageFilter.blur) + ترکیب رنگ — سبک‌تر ولی همان حس |

- ویجت مشترک: `GooeyContainer` + `GooeyItem` در `core/effects/` — کل اپ از همین دو ویجت استفاده می‌کند
- قانون: متن هرگز gooey نمی‌شود — فقط شکل ظرف‌ها؛ متن لایه جدا و همیشه شارپ

جزئیات کامل در `design.md`.

---

## ۱۰. امنیت و پایداری

- **کرش:** سناریوی خراب هرگز نباید اپ را ببندد — هر خواندن فایل در try/catch + پیام فارسی دوستانه
- **checksum:** فایل دستکاری‌شده → رد با پیام «فایل سناریو سالم نیست»
- **نسخه ناشناخته فرمت** (فایل از نسخه آینده اپ): پیام «این سناریو با نسخه جدیدتر گردون ساخته شده — اپ را به‌روز کنید»
- **سیدوینگ Random:** توزیع نقش با `Random.secure()`

---

## ۱۱. تصمیمات باز (برای نسخه‌های بعدی)

- [ ] شبیه‌ساز تعادل (فاز ۲): ربات‌های ساده تصمیم‌گیر + اجرای موتور ×۱۰۰
- [ ] آنلاین شدن: سرور گالری سناریوها + AI سناریوساز (اشتراک طلایی)
- [ ] نسخه iOS
